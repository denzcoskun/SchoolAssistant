<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.denzcoskun.schoolassistant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".weeklyschedule.activities.WeeklyScheduleActivity" />
        <activity android:name=".weeklyschedule.activities.AddLessonActivity" />
        <activity android:name=".weeklyschedule.activities.EditLessonActivity" />
        <activity android:name=".weeklyschedule.activities.MyLessonsActivity" />
        <activity android:name=".weeklyschedule.activities.AddLessonNameActivity" />
        <activity android:name=".weeklyschedule.activities.EditLessonNameActivity" />
        <activity android:name=".homework.activities.HomeworkActivity" />
        <activity android:name=".homework.activities.AddHomeworkActivity" />
        <activity android:name=".homework.activities.EditHomeworkActivity"></activity>
    </application>

</manifest>