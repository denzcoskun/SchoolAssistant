<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.denzcoskun.schoolassistant">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".project.activities.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".project.screens.homework.activities.HomeworkActivity"/>
        <activity android:name=".project.screens.weeklyschedule.activities.WeeklyScheduleActivity" />
        <activity android:name=".project.screens.weeklyschedule.activities.AddLessonActivity" />
        <activity android:name=".project.screens.weeklyschedule.activities.EditLessonActivity" />
        <activity android:name=".project.screens.weeklyschedule.activities.MyLessonsActivity" />
        <activity android:name=".project.screens.weeklyschedule.activities.AddLessonNameActivity" />
        <activity android:name=".project.screens.weeklyschedule.activities.EditLessonNameActivity" />
        <activity android:name=".project.screens.homework.activities.AddHomeworkActivity" />
        <activity android:name=".project.screens.homework.activities.EditHomeworkActivity"/>
    </application>

</manifest>